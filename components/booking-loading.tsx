import { ArrowLeft } from 'lucide-react';
import Link from 'next/link';
const steps = ['Your dates', 'Your room', 'Little extras', 'Your details', 'Review'];
export function BookingLoading() { return <div className="booking-page booking-loading wrap" aria-busy="true"><div className="booking-top"><Link href="/rooms/" className="back-link"><ArrowLeft size={16} />Back to the rooms</Link><span>ONE ROOM. A LITTLE TIME AWAY.</span></div><div className="booking-title"><h1>Make a little time.</h1><p>Your stay at Aurel House, one thoughtful detail at a time.</p></div><nav className="booking-progress" aria-label="Booking progress"><ol>{steps.map((step,i) => <li className={i === 0 ? 'current' : ''} key={step}><button disabled><span>{i+1}</span>{step}</button></li>)}</ol></nav><div className="booking-layout"><section className="booking-work"><h2>When shall we expect you?</h2><p role="status">Opening the dates and rooms saved in this browser…</p><div className="booking-loading-fields" aria-hidden="true"><div /><div /><div /><div /></div></section><aside className="booking-loading-summary" aria-hidden="true" /></div></div>; }
