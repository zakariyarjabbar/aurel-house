import { Dashboard } from '@/components/admin';
export const metadata = { title: 'Local front desk', description: 'Openly accessible hotel dashboard for this browser-only demo. Reservations, rooms, maintenance and messages share one local store.' };
export default function Page() { return <Dashboard />; }
